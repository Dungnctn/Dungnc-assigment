{
  "name": "assigment-acme",
  "version": "0.0.0",
  "scripts": {
    "start:server": "json-server --watch db.json --port 3001",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^2.7.2"
  },
  "dependencies": {
    "axios": "^0.25.0",
    "navigo": "^8.11.1"
  }
}
